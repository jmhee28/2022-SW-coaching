{{>layout/header}}
<br>
<div class="col-md-12">
    <div class="col-md-4">
        <form>
            <div class="form-group">
                <label for="title">ID</label>
                <textarea class="form-control" id="id" readonly>{{post.id}}</textarea>
            </div>
            <div class="form-group">
                <label for="title">TITLE</label>
                <textarea class="form-control" id="title" readonly>{{post.title}}</textarea>
            </div>

            <div class="form-group">
                <label for="content"> content </label>
                <textarea class="form-control" id="contents" readonly>{{post.contents}}</textarea>
            </div>
        </form>
        <a href="/" role="button" class="btn btn-secondary">go to home</a>

    </div>
</div>

{{>layout/footer}}